general:
  branches:
    ignore:
      - dev

dependencies:
  override:
    - npm install

test:
  override:
    - npm test

deployment:
  staging:
    branch: master
    commands:
      - npm run build
    heroku:
      appname: ghentsports
