general:
  branches:
    ignore:
      - dev

dependencies:
  override:
    - npm install

test:
  override:
    - npm test

deployment:
  staging:
    branch: master
    heroku:
      appname: ghentsports
